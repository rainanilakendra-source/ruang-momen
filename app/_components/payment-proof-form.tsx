"use client";
import { useActionState } from "react";
import { uploadPaymentProof, type ProofState } from "../dashboard/orders/actions";
export function PaymentProofForm({ orderId }: { orderId: string }) { const action = uploadPaymentProof.bind(null, orderId); const [state, formAction, pending] = useActionState(action, { error: null, success: false } satisfies ProofState); return <form action={formAction} className="mt-5 space-y-4"><input type="file" name="proof" required accept="image/jpeg,image/png,image/webp" className="block w-full rounded-xl border border-[#F5F0E7]/10 p-3 text-sm" /><textarea name="note" maxLength={1000} placeholder="Catatan pembayaran (opsional)" className="w-full rounded-xl border border-[#F5F0E7]/10 bg-[#071727] p-3 text-sm" />{state.error && <p className="text-sm text-red-300">{state.error}</p>}{state.success && <p className="text-sm text-emerald-300">Bukti pembayaran terkirim.</p>}<button disabled={pending} className="min-h-11 rounded-xl bg-[#D6B56F] px-5 font-bold text-[#071727]">{pending ? "Mengunggah..." : "Upload Proof"}</button></form>; }
